<!DOCTYPE html>
<html>
<head>
  <%= stylesheet_link_tag "main" %>
</head>
<body>
  <div class='centered'>
    <header>
      <h2><strong><%= @company.name %></strong></h2>
    </header>
    <%= link_to 'Edit', edit_company_path(@company), :class => 'button' %>
    <%= link_to 'Delete', company_path(@company), method: 'delete', :class => 'button' %>
    <p class='font'><%= link_to "See Jobs at #{@company.name}", company_jobs_path(@company) %></p>
    <br>
    <br>
    <br>
    <%= render partial: "contacts/form" %><br><br>
    <h4 class='font'>Existing Contacts</h4>
    <ul class="ul-pad">
      <% @contacts.each do |contact| %>
        <li> <%= contact.name %> <%= contact.role %> <%= contact.email %></li>
      <% end %>
    </ul>
  </div>
</body>
</html>
