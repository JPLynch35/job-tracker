<div id="comment_<%= comment.id %>">
  <%= link_to 'Delete', job_comment_path(@job, comment), method: 'delete', :class => 'button' %>
  <%= comment.created_at.to_time.strftime('%B %e at %l:%M %p') %> - <%= comment.content %>
</div>
